#include <stdio.h>

int main(void)
{
    int    n;
    int    p;

    printf("Enter value for n and p followed by 'Enter': ");
    scanf("%d %d", &n, &p);
    printf("n value is: %d\np value is: %d\n", n, p);

    return(0);
}
